import { Component } from '@angular/core';

@Component({
  selector: 'app-login',
  standalone: true,
  templateUrl: './login.component.html',
  styleUrl: './login.component.css'
})
export class LoginComponent {
  // TODO: form i login-it + AuthService.login(), implementohet në hapin "AuthService + login/register"
}
